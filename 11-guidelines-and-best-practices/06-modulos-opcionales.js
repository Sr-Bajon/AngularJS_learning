/*
  MODULOS OPCIONALES

  ngCookies
    Tradicionalmente, las cookies siempre han estado basadas en strings.
    ngCookies te da un servicio que permite interactuar con las cookies del browsers como un objeto.
    Puedes directamente guardar objetos con claves, y obtenerlas como objetos, en vez de tratar con
    strings y parsearlos.

  ngSanitize
    Si necesitas trabajar con contenido generado por el usuario y necesitas mostrarlo en la UI,
    te puedes exponer a ataques de tipo Cross Site Scripting (XSS). Especialmente si el usuario
    mete Javascript y HTML cuando debería introducir solo texto. ngSanitize te da varias opciones
    para lidiar con esto.

  ngResource
    La libreria $http es un recurso de bajo nivel, este servicio envuelve el servicio $http y provee
    metodos como save, query y update.
    Si tenemos un servidor RESTful podemos usar el modulo ngResource y el servicio $resource.

  ngTouch
    Permite lidiar con UI tactiles.

  ngAnimate
    Permite incluir animaciones para los eventos de AngularJS (ngHide y ngShow por ejemplo)





*/
